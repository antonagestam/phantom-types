# https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2
sphinx:
  configuration: docs/conf.py
python:
  # Higher than 3.8 isn't supported yet.
  version: "3.8"
  install:
    - requirements: docs/requirements.txt
